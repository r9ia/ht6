# Dread Director — Agent Handoff

Updated: 2026-07-18 10:58 EDT

## Immediate next action

Unity received a final refresh after adding automatic idle-animation wiring for the imported Demon. The subsequent verification command was cancelled by the user, so **do not assume that last compile/rebuild completed**.

1. Confirm Unity is idle and still open on `C:\Users\Karan\ht6`.
2. Inspect `Logs/Editor.log` for:
   - `Creature animation wired with clip ...`
   - `Added optional creature prefab from Assets/ThirdParty/Quaternius/UltimateMonsters/Demon/Demon.fbx.`
   - `Scene validation passed: player movement ...`
   - any `error CS`, `InvalidOperationException`, FBX import, or shader errors.
3. Verify these exist:
   - `Assets/Generated/DreadDirector/ImportedCreature.controller`
   - a reference to that controller in `Assets/Scenes/DreadDirectorNightWatch.unity`.
4. If the revisioned bootstrap did not rebuild, focus Unity and send Refresh (`Ctrl+R`). `DreadDirectorSceneBootstrap` revision is currently `cc0-demon-v2`, which should rebuild once.
5. Enter Play mode and send an escalation (`2` or UDP JSON) to verify the Demon appears, animates, and hides/re-arms correctly.
6. Stop Play mode and confirm UDP port 7777 is released.

## User and working style

- User: Karan, Hack the 6ix 2026.
- Be concise and action-oriented; prioritize a working MVP.
- Generate and wire Unity content programmatically. Do not ask Karan to manually place objects or drag Inspector references.
- Do not commit or push unless explicitly asked.
- Do not stage, restore, edit, or commit `Assets/TutorialInfo/Icons/URP.png`; it was already modified immediately after clone due an upstream Git LFS mismatch.
- Preserve unrelated working-tree changes.

## Active repository

- Root: `C:\Users\Karan\ht6`
- GitHub: `https://github.com/r9ia/ht6`
- Branch: `dev`
- Base commit: `cc02388`
- Unity: `6000.5.4f1`, URP 17.5.0
- Unity project root is this repository (`Assets`, `Packages`, `ProjectSettings`).
- No changes are staged. No commits or pushes were made by this agent.

## Current Unity MVP

Generated scene:

- `Assets/Scenes/DreadDirectorNightWatch.unity`
- Generated by `Assets/Editor/DreadDirectorSceneBootstrap.cs`
- Menu: **Dread Director → Build Night Watch Scene**
- The bootstrap also performs a revisioned one-time rebuild using `EditorPrefs`.

Working features:

- Imported Backrooms environment.
- Generated player with `CharacterController`, WASD movement, mouse look, and `Esc` cursor toggle.
- 60-second calibration controller.
- HUD showing high-level Director state.
- Light flicker/tension effects.
- Hidden apparition, procedural sting, panic retreat, and recovery re-arm.
- Keyboard fake events: `1` low escalation, `2` high escalation, `3` panic/back-off, `4` recovery.
- UDP high-level JSON on port 7777.
- Fake and UDP paths both route through `DirectorGameBridge`.
- Optional localhost narration bridge with offline fallback.

Validated before the final animation edit:

- Unity C# compiler reported no errors.
- Bootstrap logged that player movement, camera, UDP/fake routing, effects, HUD, narration, and hidden apparition were wired.
- Play mode logged UDP listening and escalation routing.
- Offline narration returned successfully.
- UDP 7777 was released after Play mode stopped.

## Monster integration

The Unity Asset Store `Creep Horror Creature` was not present in Karan's cache or any team branch. It could not be acquired automatically because Asset Store entitlement/EULA acceptance requires Karan's Unity account.

A directly downloadable licensed alternative was imported:

- Creature: **Demon** by Quaternius
- Model page: `https://poly.pizza/m/Mo2ky6vkf8`
- Bundle page: `https://poly.pizza/bundle/Ultimate-Monsters-Bundle-5oyGWAmOB6`
- License: CC0 1.0
- Imported FBX: `Assets/ThirdParty/Quaternius/UltimateMonsters/Demon/Demon.fbx`
- License/source record: `Assets/ThirdParty/Quaternius/UltimateMonsters/LICENSE.md`
- Download archive SHA-256: `E3645DCA06DC4AD68E7C6ACB03CB2CD99736F6750F10671CE92EC2B26CF605D9`
- FBX SHA-256: `FB3966E79D2C44F67A5D9E7823FA58ED7C9D10942F334C96ADF1C9EB4B492664`
- Only one FBX was retained; no ZIP and none of the other 44 bundle models were copied into the project.
- `.gitattributes` applies Git LFS to the FBX.

Verified before adding automatic animation:

- Unity imported the FBX as a Generic rig (`animationType: 2`) with `importAnimation: 1`.
- Scene had 16 references to the Demon FBX GUID.
- `Hidden Apparition Visual` had `m_IsActive: 0` in the saved scene.
- Scene contained the `Imported Creature` prefab override.
- Unity logged `Added optional creature prefab ... Demon.fbx`.

Latest unverified edit:

- `DreadDirectorOptionalAssets.ConfigureCreatureAnimation` scans FBX sub-assets, prefers a clip containing `idle`, creates/updates `Assets/Generated/DreadDirector/ImportedCreature.controller`, and assigns it to an Animator.
- Bootstrap revision was bumped from `cc0-demon-v1` to `cc0-demon-v2`.
- A Unity Refresh was sent, but the verification tool was cancelled.

## Backrooms environment

Karan's teammate Georgia pushed `origin/georgia` at `4abee77`.

The branch contained:

- two duplicate Backrooms package trees;
- a useful basic first-person controller;
- an unrelated approximately 1.1 GB terrain demo;
- personal Unity cloud/project settings.

A wholesale merge was intentionally avoided. Only the correct environment package was restored through an isolated sparse worktree:

- `Assets/LoafbrrAssets/BackroomsLikeAssetRe`
- 679 files
- approximately 294.5 MiB in the working tree
- all 60 Git LFS objects materialized; zero pointer stubs
- included README declares CC0
- duplicate `Assets/Asset/BackroomsLikeAsset` was not imported
- `Assets/TerrainDemoScene_URP` was not imported

The bootstrap deterministically selects:

`Assets/LoafbrrAssets/BackroomsLikeAssetRe/prefab/Level/TstLevel.prefab`

Unity previously logged that this prefab was added, and the generated scene contained 15 references to its GUID.

## Important Unity files

Runtime:

- `Assets/Scripts/Network/DirectorMessage.cs`
- `Assets/Scripts/Network/DirectorUdpReceiver.cs`
- `Assets/Scripts/Director/DirectorGameBridge.cs`
- `Assets/Scripts/Director/FakeDirectorInput.cs`
- `Assets/Scripts/Director/CalibrationController.cs`
- `Assets/Scripts/Horror/LightFlicker.cs`
- `Assets/Scripts/Horror/ApparitionController.cs`
- `Assets/Scripts/Horror/AudioStingController.cs`
- `Assets/Scripts/UI/BiometricDebugHud.cs`
- `Assets/Scripts/Presentation/NarrationBridgeClient.cs`
- `Assets/Scripts/Player/FirstPersonController.cs`

Editor:

- `Assets/Editor/DreadDirectorSceneBootstrap.cs`
- `Assets/Editor/DreadDirectorOptionalAssets.cs`

Generated:

- `Assets/Scenes/DreadDirectorNightWatch.unity`
- `Assets/Generated/DreadDirector/*.mat`
- expected after final verification: `Assets/Generated/DreadDirector/ImportedCreature.controller`

## Monster behavior

`ApparitionController` is already implemented:

- starts hidden;
- reveals once on escalation;
- visibility duration scales with intensity;
- retreats on panic;
- re-arms on recovery;
- imported creature and procedural fallback share the same logic.

A chase AI/combat/navigation system is intentionally not part of the first MVP.

## Narration bridge

Location: `narration-bridge/`

- TypeScript, loopback-only.
- Accepts allowlisted event labels only.
- Offline curated line bank works without credentials.
- Optional Gemini text and ElevenLabs speech are outside QNX and never receive biometrics.
- `npm test` previously passed 3/3.
- HTTP smoke previously returned health `ok`, offline narration, and HTTP 400 for an unsupported event.

Commands:

```powershell
npm --prefix narration-bridge install
npm --prefix narration-bridge test
npm --prefix narration-bridge start
```

## Secrets

Karan pasted Gemini and ElevenLabs keys into chat. Treat both as exposed and require rotation.

- The exposed values were never written to the repository.
- Root `.env` exists locally with blank values and is ignored.
- `.env.example` contains placeholders only.
- A prior source scan found no exposed key patterns.

## Documentation imported

- `README.md`
- `AGENTS.md`
- `docs/ARCHITECTURE.md`
- `docs/PROJECT_SCOPE.md`
- `docs/UNIFOLD_INTEGRATION.md`
- `docs/THIRD_PARTY_ASSETS.md`

Architecture truth:

- SmartSpectra runs on a supported Linux sensor host, not QNX.
- QNX runs portable C++17 Director logic and optional local OpenCV DNN.
- Unity consumes high-level Director events only.
- `director-core` must not include Unity, SmartSpectra, OpenCV, sockets, or QNX headers.

## Repository caution

The working tree contains unrelated/pre-existing Unity changes. Do not clean, restore, stage, or commit them casually. Known examples include:

- `.vscode/settings.json`
- render-pipeline assets
- several `ProjectSettings` files
- `Assets/TutorialInfo/Icons/URP.png` (protected LFS mismatch)
- Unity's intentional self-deletion of `Assets/Editor/HubForceResolve.cs`

Also:

- `My project/` is an accidental nested Unity project and is ignored.
- generated `.slnx` files are ignored.
- no files are staged.

Before any future commit, stage explicit intended paths only and exclude all unrelated settings/icon changes.

## Remaining project work after monster verification

Unity MVP:

1. Visually verify Demon scale/orientation/materials/idle animation during reveal.
2. Check player starts inside usable Backrooms geometry and can move without clipping.
3. Produce a standalone build and rehearse fake keys + UDP fallback.

Broader SDK work:

1. Portable C++17 `director-core` and deterministic tests.
2. QNX UDP receiver/emitter and replay path.
3. Linux SmartSpectra adapter.
4. Optional OpenCV DNN only after heuristic fallback.
5. Unifold sandbox bridge after core vertical slice stability.

Do not expand into combat, chase AI, large-level design, or cloud-dependent real-time logic before the end-to-end MVP is stable.
